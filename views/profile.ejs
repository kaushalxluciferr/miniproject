<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script> 
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <div class="kaa">
    <a href="/profile/upload" class="upload"> upload your profile</a>
    <a class="kaushal" href="/logout">Click to  Logout</a>
</div>
<div class="flex">

<div class="w-10 h-10">
    <img class="w-full h-full object-cover overflow-hidden" src="/images/uploads/<%=user.profilepic%>" alt="">
</div>
    <h1 class="headd">Welcome, <span class="text-red-900"><%=user.name%></span>👋</h1>
</div>

<h5 class="mb-5">You can create a new Post</h5>
<form action="/post" method="post">
    <textarea placeholder="What's on your mind?" class="p-5 outline-none w-1/2  resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="text" id=""></textarea>
    <input class="w-40 mt-5 px-3 py-2 bg-blue-600 block" type="submit" value="Upload your post">
</form>
<div class="post mt-100">
    <h3 class="text-zinc-400 mb-10">Your post are below</h3>
<div class="postcontainer ">
<% user.post.reverse().forEach(function(postt)   <!-- display every post in latest -->
{
    
%>
<div class="post mb-2 w-1/3 p-4 border-2 bg-zinc-800 border-blue-600">
    <h4 class="text-red-500 mb-2"><%=user.username%></h4>
    <p class="text-sm tracking-tighter"><%=postt.content%></p>
    <small class="like text-yellow-500"><%=postt.likes.length%> likes</small>
    <div class="btns flex gap-20 mt-5">
        <a class="text-blue-500" href="/like/<%=postt._id%>"><%=postt.likes.indexOf(user.id)===-1?"Like":"Unlike"%> </a>
        <a class="text-green-500" href="/edit/<%=postt._id%>">Edit</a>
        <a class="text-green-500" href="/delete/<%=postt._id%>">delete</a>

    </div>
    </div>
<%
})%>

</div>
</div>

  </div>
    
</body>
</html>